export interface Tournament {
    title: string,
    description: string,
    teamQuantity: number,
    playerCapacity: number,
    prizePool: number,
    tDate: string,
    createdAt: string,
    tHour: string,
    isTeamMode: false,
    id: number,
    userId: number,
    gameId: number
    urlToImage: string

}
