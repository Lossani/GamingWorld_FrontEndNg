export interface Tournament {
    title: string,
    description: string,
    teamQuantity: number,
    playerCapacity: number,
    prizePool: number,
    createdAt: string,
    isTeamMode: false,
    id: number,
    userId: number,
    gameId: number,
    urlToImage: string,
    tournamentDate: string,
    tournamentStatus: boolean

}
